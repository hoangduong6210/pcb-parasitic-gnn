# Quarantined historical corpus

`synth_v2` is preserved only for reproducibility of the data-integrity audit.
Its generator can produce contradictory layer/z coordinates and overlapping
conductor volumes, while its mixed analytical/filament labels violate the
inductance passivity condition. These files are not ground truth and must not
feed current training or scientific claims.

Use the v3 pipeline documented in the repository root instead.
